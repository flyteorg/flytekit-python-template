#!/usr/bin/env bash

# Iterates over all templates and runs them, waits for the result to complete and checks if there was an error.

# If there was an error, it will print the error and exit with a non-zero exit code.

# If there was no error, it will print the result and exit with a zero exit code.


